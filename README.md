# RefractoredVersionRepo

Standalone extraction of the `RefractoredVersion` highway simulation code.

## Build

```powershell
.\gradlew.bat build
```

## Run

```powershell
.\gradlew.bat run
```

The default runner is `RefractoredVersion.TestScript.TestScripts`.
